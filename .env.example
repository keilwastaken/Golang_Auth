# MongoDB connection string
MONGOURI=mongodb://localhost:27017/goauth

# Server configuration
SERVERADDRESS=:8080

# JWT token lifespans
# Format: number followed by unit (s, m, h)
# Examples: 15m = 15 minutes, 24h = 24 hours, 7d = 7 days
ACCESS_TOKEN_LIFESPAN=15m
REFRESH_TOKEN_LIFESPAN=7d

# JWT Signing Key (generate a strong random string)
# IMPORTANT: Use a long, random string in production
# Example: openssl rand -base64 32
JWT_SIGNING_KEY=your-super-secret-jwt-signing-key-here